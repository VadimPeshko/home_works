{
  "name": "hw6",
  "version": "1.0.0",
  "description": "run server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "http-server -a localhost -p 8000 -c-1 -o http://localhost:8000/public_html/index.html",
    "start-json": "json-server --watch ./public_html/db/db.json --port 3006"
  },
  "author": "Vadim Peshko",
  "license": "ISC",
  "dependencies": {
    "http-server": "^0.11.2",
    "json-server": "^0.15.1"
  },
  "devDependencies": {}
}